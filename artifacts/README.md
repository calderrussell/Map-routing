# Generated evidence

Milestone audits are versioned here. Runtime JSON, checkpoints, tables, and figures
belong under `artifacts/runs/` and are gitignored because they must be regenerated from
the frozen configuration and recorded seeds.

